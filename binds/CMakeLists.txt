# Bindings are intentionally disabled for now.
# We will re-enable them after the core ABI/build/test cleanup.

# add_subdirectory(cpp)
# add_subdirectory(py)

# if(QE6502_BUILD_WASM)
#     add_subdirectory(js)
# endif()
